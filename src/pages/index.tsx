import { Box } from "@chakra-ui/layout";
import MainSection from "components/layout/MainSection";

const Home = () => {
  return (
    <Box w="full">
      <MainSection />
    </Box>
  );
};

export default Home;
